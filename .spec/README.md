# `.spec`

This folder is the Spec Led Development layer for `specled_ex`.

<!-- covers: spec.workspace.readme_present spec.workspace.reconcile_loop -->

This project uses its own tooling to specify itself.

## Layout

- `README.md` — this file (authored)
- `AGENTS.md` — local operating guidance for agents working in `.spec/` (authored)
- `decisions/README.md` — ADR guidance for durable cross-cutting decisions (authored)
- `decisions/*.md` — accepted or superseded ADRs for the current workspace (authored)
- `specs/spec_system.spec.md` — meta-spec for this workspace (authored)
- `specs/package.spec.md` — package-level contract (authored)
- `specs/index_state.spec.md` — index building and canonical state rules (authored)
- `specs/block_schema.spec.md` — structured block schema rules (authored)
- `specs/parser.spec.md` — parser module spec (authored)
- `specs/verification.spec.md` — verification workflow spec (authored)
- `specs/mix_tasks.spec.md` — Mix task workflow spec (authored)
- `specs/decision_governance.spec.md` — ADR parsing and validation rules (authored)
- `specs/status.spec.md` — status and coverage summary rules (authored)
- `specs/branch_guard.spec.md` — branch-aware co-change validation rules (authored)
- `specs/next.spec.md` — guided reconciliation rules for current Git changes (authored)
- `specs/prime.spec.md` — session-start context rules for agents and maintainers (authored)
- `state.json` — generated by `mix spec.validate`

## Workflow

1. If you are entering the repo or handing work to an agent, run `mix spec.prime --base HEAD`.
2. Make the smallest code, docs, or test change that matters.
3. Add or tighten the smallest proof when behavior changed.
4. Run `mix spec.next` to reconcile the current Git change set.
5. If next says `needs subject updates`, update current-truth subjects in `specs/`.
6. If next says `needs decision update`, update `decisions/*.md` only when the change should stay durable.
7. When next says `ready for check`, run `mix spec.check --base ...` for the full local gate.
8. Use Git history and pull requests as the time dimension for how a change evolved.
9. Use `mix spec.status` when you want coverage and weak-spot summaries.
10. Use `mix spec.index` and `mix spec.validate` only when you need low-level plumbing output.

## Notes

- Structured blocks prefer YAML, though JSON also parses.
- Verification kinds use `source_file`, `test_file`, etc. (not `test` or `doc`)
- Subject specs may reference ADR ids through `spec-meta.decisions`.
